# Vestigant Spotlight V1.3.5 Release Notes

V1.3.5 focuses on iOS investigator value, with communication identity and KnowledgeC expansion first. It adds WEB_DOWNLOADS app-database parsing, bounded KnowledgeC target-hint promotion, and protected-data/keychain-material candidate correlation without asserting decryption or unsupported intent.

Validation in this environment: Linux CMake build passed, CLI reports v1.3.5, and self-test passed. Windows/MSVC build and iOS thin validation are required.
