Vestigant Spotlight v0.9.26.2 change/validation note

Change class: compile fix.

Trigger:
- Windows/MSVC v0.9.26.1 built CLI and self-tests but failed GUI compilation:
  src\gui\win32_gui.cpp(1243): error C2026: string too big, trailing characters truncated

Fix:
- Added execGuiSqlParts(...) runtime SQL assembly helper in src/gui/win32_gui.cpp.
- Split oversized GUI SQL raw string literals into smaller R"VSGUI(...)VSGUI" segments.
- Preserved v0.9.26.1 GUI review SQL/view behavior.
- Bumped version metadata/scripts to v0.9.26.2.

Static validation:
- src/gui/win32_gui.cpp maximum raw string literal body after patch: <= 7,000 bytes target.
- src/db/case_db.cpp maximum raw string literal body remains below prior verified threshold.

Required external validation:
- Run Windows/MSVC build because the original failure is MSVC-specific and Linux does not compile win32_gui.cpp.
