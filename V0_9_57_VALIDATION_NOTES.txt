V0_9_60 validation notes

Scope:
- Focused Windows/MSVC build-script hotfix after V0_9_55 failed with:
  "The system cannot find the batch label specified - CompileCommon".
- No parser, ingest, GUI layout, cache, ZIP, FFS inventory, app database, or forensic interpretation logic was intentionally changed.

Root cause classification:
- The V0_9_55 no-CMake MSVC batch file used repeated CALL :CompileCommon subroutine labels.
- On the user's Windows run, CMD failed during the common-object compile sequence after sqlite_enrichment.cpp, before sqlite_exporter.cpp, with a missing CompileCommon label error.
- This is a build-script control-flow fragility issue, not a C++ compile failure.

Changes made:
- Updated version metadata to 0.9.60.
- Replaced build_windows_msvc.bat subroutine-label common compilation with a manifest-driven FOR /F loop.
- Removed :CompileCommon, :CompileCommonQuiet, and :RequireObject labels from build_windows_msvc.bat.
- Added explicit object-existence validation for case_db.obj, case_store.obj, and app_runner.obj.
- Wrote build_windows_msvc.bat with CRLF line endings.

Validation performed in this environment:
- Verified build_windows_msvc.bat no longer contains CompileCommon or RequireObject labels/calls.
- Verified build_windows_msvc.bat is CRLF text.
- Ran CMake configure successfully on Linux.
- Ran Linux CMake build successfully for core library, CLI, and tests.
- Ran VestigantSpotlightTests self-test successfully.
- Verified version string reports 0.9.60 in source metadata.
- Packaged full source ZIP and patch ZIP and verified ZIP integrity.

Validation not completed here:
- Windows/MSVC build-script execution.
- GUI runtime validation.
- Existing-case tag/view-set/details-pane runtime validation.

Recommended Windows validation:
1. Build V0_9_60 with scripts\Build-V0_9_60.ps1.
2. Confirm build_windows_msvc.bat compiles past sqlite_enrichment.cpp into sqlite_exporter.cpp, case_store.cpp, app_runner.cpp, CLI/test/GUI links, and prints Build complete.
3. Launch GUI and confirm V0_9_55 GUI changes remain present.
