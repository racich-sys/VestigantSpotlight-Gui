Vestigant Spotlight V0_9_57 validation summary

Scope:
- V1 production-readiness cleanup based on the user's surgical strike plan.
- GUI-only investigator-surface cleanup plus removal of compiled legacy V7 importer path and migration of GUI SQL view creation out of win32_gui.cpp.
- No parser, ZIP staging, FFS inventory, app DB parsing, cache, or forensic interpretation logic was intentionally changed.

Implemented:
- Removed the GUI "Skip preservation (temporary testing only)" checkbox.
- GUI run mode now always preserves evidence: opt.preserveEvidence=true and opt.preserveEvidenceExplicit=true.
- Removed GUI "Self-test" mode dropdown option; self-test remains available from CLI/tests, not the investigator GUI.
- Removed GUI Max records / Blocks per store diagnostic limit boxes; GUI normal runs use unlimited native record/block limits.
- Removed visible Legacy V7 workflow text from the GUI and CLI help.
- Removed compiled legacy V7 importer source/header from the package.
- Removed V7 importer compilation from build_windows_msvc.bat and CMakeLists.txt.
- Removed import-v7/legacy-v7 command-line parsing and runApplication import/compare branches.
- Moved ensureGuiReviewViews SQL creation from src/gui/win32_gui.cpp to CaseDatabase::ensureGuiReviewViews() in src/db/case_db.cpp.
- ReadOnlyDb no longer creates/upgrades GUI SQL views during read-only review opening; existing-case open still calls CaseDatabase::initializeSchema() first when writable.
- Preserved the V0_9_53 true two-column selected-row details ListView and tab visibility behavior.
- Kept legacy v7_* schema columns/tables for older-case compatibility only; no importer path is compiled or exposed.

Validation performed in this environment:
- g++ C++20 syntax check: src/db/case_db.cpp passed.
- g++ C++20 syntax check: src/app/app_runner.cpp passed.
- g++ C++20 syntax check: src/enrich_sql/sqlite_enrichment.cpp passed.
- g++ C++20 syntax check: src/cli/main.cpp passed.
- g++ C++20 syntax check: src/core/app_info.cpp passed.
- CMake configure passed.
- Linux CMake build passed for core library, CLI, and tests.
- VestigantSpotlightTests self-test passed.
- Verified v7_output_importer.cpp/h are absent from the source package.
- Verified CMakeLists.txt and build_windows_msvc.bat no longer reference v7_output_importer.
- Verified GUI source no longer references gNoPreserve, gMaxRecords, gMaxBlocks, Skip preservation, Self-test, Max records, or Blocks/store.

Validation not completed here:
- Windows/MSVC GUI build/link.
- Runtime GUI confirmation that Case Information tab remains free of row-detail controls.
- Runtime GUI confirmation that selected-row details pane remains a true Field / Metadata table.
- Existing-case GUI view upgrade/open test on the user's Windows case database.

Recommended Windows validation:
1. Build V0_9_57 on Windows.
2. Launch GUI and confirm Case Information no longer shows Skip preservation, Self-test, Max records, or Blocks/store controls.
3. Open existing completed case.
4. Confirm MacOS/iOS Investigation tabs still show View Set selector and useful views.
5. Confirm selected-row details pane appears only on MacOS/iOS investigation tabs and remains a two-column Field / Metadata table.
6. Confirm no V7 import/compare option appears in the GUI or CLI help.
