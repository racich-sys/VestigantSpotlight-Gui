V1.6.79 code review implementation notes

User-provided review items assessed against V1.6.78 source:
1. O(N*M) AFF4/APFS Spotlight copy-attempt correlation: verified in src/parsers/aff4_probe_worker.cpp. Implemented O(N+M) lookup using an unordered_map keyed by (volumeSequence, childFileId).
2. ANALYZE timing in SQLite enrichment: verified that ANALYZE occurred before artifacts were repopulated. Added post-artifact-insert ANALYZE for artifacts, artifact_source_instances, and raw_records before parent-inode recursive reconstruction.
3. TEXT-typed inode/store keys: valid architectural concern but not changed in V1.6.79 because it requires schema migration and view/query compatibility review. Deferred with first-fix guidance.
4. Cycle guard string/instr in recursive CTE: retained because the recursion cap is 20 and the guard is correct; future optimization should be evidence-driven.
5. raw_key_values EAV expansion: valid known scalability issue. FullValues remains opt-in; CoreFields remains default.
6. ApfsVolumeReader/production traversal duplication: valid maintainability concern but not changed in V1.6.79 because it requires careful APFS runtime equivalence testing.
7. Sanitizer builds: valid testing enhancement; deferred.
8. Unresolved-object full local index: already implemented in V1.6.78 and verified as materially helpful on the uploaded V1.6.78 result.
9. unused-function warnings: valid maintainability issue; deferred cleanup because removing code during AFF4 validation risks deleting staged diagnostics.
10. static analyzer gate: deferred.
11. large-file modularization: continuing guidance, not changed in V1.6.79.

Additional fix:
- Parent-inode path reconstruction self-test now inserts synthetic raw_key_values rows so the required self-test covers the non-empty raw_key_values path and should no longer emit the previous warning in that test.
